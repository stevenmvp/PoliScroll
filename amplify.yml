version: 1
applications:
  - appRoot: apps/amplify-smoke
    frontend:
      phases:
        build:
          commands:
            - mkdir -p dist
            - cp index.html dist/index.html
      artifacts:
        baseDirectory: dist
        files:
          - '**/*'
