# Amplify smoke test

Módulo estático y aislado para comprobar que Amplify puede instalar, construir y publicar un artefacto del repositorio.

No comparte código con `apps/web` y no reemplaza la aplicación principal. La configuración activa temporalmente este módulo desde el `amplify.yml` raíz.
